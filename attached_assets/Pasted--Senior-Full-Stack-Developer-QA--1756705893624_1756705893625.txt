Представь, что ты — Senior Full‑Stack Developer + QA‑инженер. Твоя задача — довести проект до состояния идеальной копии https://pc.dfbiu.com/ с независимой серверной частью на PHP и своей БД, но с сохранением только пользовательских данных у нас.

Этап 1 — Аудит текущего состояния
Пройтись по всем страницам проекта (включая авторизованные) и зафиксировать:

Что уже реализовано.

Что отсутствует или работает некорректно.

Составить список несоответствий с эталоном:

Визуал (пиксель‑в‑пиксель).

UI/UX поведение.

Hover‑эффекты, модальные окна, анимации.

Регистрация/авторизация.

Хранение данных (должно быть только у нас).

Зафиксировать ошибки в консоли и проблемы с загрузкой ассетов.

Этап 2 — Реализация
Фронтенд

Пиксель‑в‑пиксель совпадение с эталоном.

Все hover‑эффекты, модальные окна, анимации работают как на оригинале.

При hover на пункты меню в header — модалки появляются и работают идеально.

Все интерактивные элементы реагируют так же, как на эталоне.

Бэкенд (PHP)

Реализовать регистрацию и авторизацию через нашу БД.

Убрать любые обращения к API оригинала.

Хранить только пользовательские данные (логин, пароль, ник, баланс, статус).

Сессии и авторизация — через PHP (cookies/$_SESSION).

База данных

Таблица users с полями: id, username, password_hash, nickname, balance, status, created_at.

Возможность управлять пользователями через админку (создание, изменение, удаление, изменение баланса).

Админка

Интерфейс для управления пользователями.

Возможность менять ник, баланс, статус.

Доступ только для администратора.

Этап 3 — Проверка
Пройтись по всем страницам эталона и сравнить с нашим проектом:

Визуал — пиксель‑в‑пиксель.

Поведение — идентично.

Hover‑эффекты, модалки, анимации — работают идеально.

Проверить:

Регистрация/авторизация работают через наш сервер.

Данные сохраняются в нашей БД.

Нет запросов к оригинальному сайту.

Нет ошибок в консоли.

Все ассеты загружаются без 404.

Проверить адаптивность и кроссбраузерность.

Этап 4 — Запуск
Запустить приложение на локальном сервере.

Пройти полный сценарий:

Регистрация нового пользователя.

Авторизация.

Проверка отображения авторизованного UI.

Hover‑эффекты и модалки.

Изменение данных пользователя через админку.

Убедиться, что всё работает идеально.